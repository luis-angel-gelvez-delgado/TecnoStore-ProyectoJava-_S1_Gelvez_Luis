package Modelo;

// esto es un enum, que es como una lista fija de valores posibles
// en este caso, las gamas o categorias de celulares
// se usa para evitar errores de escritura (ejemplo: escribir "altta" en lugar de "ALTA")
public enum CategoriaGama {

    // las tres gamas posibles de un celular
    // alta: celulares premium (iPhone, Galaxy S, etc)
    // media: celulares de rango medio 
    // baja: celulares economicos o basicos
    ALTA,
    MEDIA,
    BAJA
    
}